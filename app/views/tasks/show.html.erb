<p><strong>Title:</strong> <%= @tasks.title %></p>
<p><strong>Details:</strong> <%= @tasks.details %></p>
<p><strong>Completed?:</strong> <%= @tasks.completed %></p>

<div class="a pt-3">
<%= link_to "Go back", tasks_path, {class: "btn-orange"}%>
<%= link_to "Update a Task 🪸", edit_path, {class: "btn-blue"} %>
<%= link_to "Delete a Task 🗑", destroy_path(@tasks), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</div>
